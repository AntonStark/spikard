#include "basemath.hpp"

/*
 * В исходной строке:
 * - переменные записываются ${<имя>} типа ${A} ${Real}
 * - операции и кванторы - [<операция>] типа [exist] [all] [*] [sum]
 * ничего другого в строке быть не должно, иначе exception
 */
Predicat::Predicat(std::string)
{

}